#include<stdio.h>
#include<string.h>
void main()
{
    char str[]="hello";
    char str1[]="hello world";
     printf("compare two strings %d",strcmp(str,str1));
    // printf("compare two strings %d",strncmp(str,str1));
    
}